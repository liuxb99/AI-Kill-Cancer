name: Local Verification Gate

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ai-kill-cancer-local-verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  local-verify:
    runs-on: [self-hosted, Windows, X64, ai-ci]
    timeout-minutes: 45
    env:
      APP_MODE: research
      DB_BACKEND: sqlite
      SQLITE_PATH: ./.ci/ai-kill-cancer.db
      MODEL_ENABLED: 'false'
    defaults:
      run:
        shell: powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
    steps:
      - uses: actions/checkout@v4

      - name: Select local Python 3.10+
        run: |
          $python = Get-Command python -ErrorAction SilentlyContinue
          if (-not $python) {
            throw 'Python is not available on the self-hosted runner.'
          }

          $pythonExe = $python.Source
          $version = & $pythonExe -c "import sys; print(f'{sys.version_info.major}.{sys.version_info.minor}')"
          & $pythonExe -c "import sys; raise SystemExit(0 if sys.version_info >= (3, 10) else 1)"
          if ($LASTEXITCODE -ne 0) {
            throw 'Python 3.10+ is required on the self-hosted runner.'
          }

          $supportsPipeline = (& $pythonExe -c "import sys; print('true' if sys.version_info >= (3, 11) else 'false')").Trim()
          "PYTHON_EXE=$pythonExe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "PYTHON_SUPPORTS_PIPELINE=$supportsPipeline" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host "Python $version; full pipeline tests: $supportsPipeline"

      - name: Install SQLite verification dependencies
        run: |
          & $env:PYTHON_EXE -m pip install --disable-pip-version-check --prefer-binary `
            "pydantic>=2.10.3" "pydantic-settings>=2.7.0" `
            "sqlalchemy[asyncio]>=2.0.36" "aiosqlite>=0.20.0" `
            "httpx>=0.28.1" numpy pytest pytest-asyncio ruff

      - name: Compile backend
        run: |
          & $env:PYTHON_EXE -m compileall src/backend/config.py src/backend/database src/backend/domain src/backend/api

      - name: Lint backend and SQLite regression
        run: |
          & $env:PYTHON_EXE -m ruff check src/backend tests/backend/test_sqlite_local_compat.py tests/test_analysis_persistence.py
          & $env:PYTHON_EXE -m pytest tests/backend/test_sqlite_local_compat.py -v --tb=short
          if ($env:PYTHON_SUPPORTS_PIPELINE -eq 'true') {
            & $env:PYTHON_EXE -m pytest tests/test_analysis_persistence.py -v --tb=short
          } else {
            Write-Host 'Skipping analysis persistence runtime test on Python 3.10; pipeline runtime requires Python 3.11+.'
          }

      - name: Initialize configured local SQLite database
        run: |
          @'
          import asyncio
          from src.backend.config import settings
          from src.backend.database.session import close_db, init_db

          async def main():
              await init_db(settings.DATABASE_URL)
              await close_db()

          asyncio.run(main())
          '@ | & $env:PYTHON_EXE -

      - name: Verify local database file
        run: |
          if (-not (Test-Path '.ci/ai-kill-cancer.db')) {
            throw 'SQLite verification database was not created'
          }
          $item = Get-Item '.ci/ai-kill-cancer.db'
          if ($item.Length -le 0) {
            throw 'SQLite verification database is empty'
          }
          Write-Host "SQLite DB bytes: $($item.Length)"

      - name: Diff check
        run: git diff --check
