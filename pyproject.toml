[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["node_modules", "dist", ".git", "venv", "__pycache__"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["F", "E", "W", "I", "N", "UP"]
ignore = ["E501"]
